//! Cross-target IR fixtures: worked examples every target's compiled-vector
//! check and unit tests share, so one model proves the same construct in
//! Go, TypeScript and Rust instead of three copies drifting apart.

pub mod handle_source;
pub mod per_lang_handle;
