(tests
 (names analysis_test analysis_ext_test)
 (modules analysis_test analysis_ext_test)
 (libraries tono_lsp_lib tono_frontend lsp alcotest))

; The example ext block is the outline fixture, copied next to the test.

(rule
 (copy ../../examples/config-lib/service.tono service.tono))

; Protocol-level regressions can only be caught against the real server loop,
; so this suite drives the built binary over a pipe.

(tests
 (names server_test)
 (modules server_test)
 (libraries yojson unix alcotest)
 (deps ../tono_lsp.exe))
