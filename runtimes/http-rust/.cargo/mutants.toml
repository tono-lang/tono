# Mutation testing scope: the whole hand-written seam. This crate is entirely
# bespoke (there is no mechanical/generated code here to exclude, unlike the
# backend's own gate), so every module carries real logic a surviving mutant
# would mean a behavior no test pins down.
examine_globs = [
    "src/descriptor.rs",
    "src/request.rs",
    "src/retry.rs",
    "src/runtime.rs",
]
